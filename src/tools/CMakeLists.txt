# ============================================================================
# Development Tools
# Language bindings, benchmarks, tracing, and examples
# ============================================================================

add_subdirectory(trace)
add_subdirectory(bindings)
add_subdirectory(benchmark)
# Note: examples/ contains reference assembly implementations (no build needed)
