# Domain Flow Architecture (DFA) Compute Tile Test and Characterization Patterns

This directory contains multi-fidelity simulation and calibration of domain flow architecture (dfa) compute tile subsystems.
